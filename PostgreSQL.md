# How to Setup PostgreSQL Development

Install `postgresql`
Start `postgresql`

Confirm with:
```
pg_isready
```

Create a user called `myuser`:
```
createuser -s myuser
```

Compile and Run `CreatePostgresDB.java`
